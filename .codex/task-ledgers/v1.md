# Networking CRM v1

- Scope: personal single-user CRM from the attached brief.
- Repository: `C:\repo\networking-crm-workspace\networking-crm` with a `.git` file and Git data in the same workspace.
- Stack exception: one Next.js app on Vercel talks directly to Neon using the serverless driver. The product brief explicitly excludes a separate API and worker.
- Delivery checklist: schema, auth, contacts, interactions, recommendations, snooze, CSV import, agent API, OpenAPI, tests, README, GitHub/Neon/Vercel deployment, browser verification.
- Deployment owner: root agent. Persistent production resources only; no PR preview required for this greenfield first release.
- GitHub repository: `NathanPannell/networking-crm` (private). Source remote added; push pending final build.
- Neon project: `bold-math-31800795`; production branch `br-lively-bird-ak3aaqnt`; development branch `br-ancient-sunset-akzc78m0`. Production is empty. Development branch has fictional seed contacts.
- Neon production endpoint: `ep-shiny-dew-akficz66`, fixed at 0.25 CU with plan-default scale-to-zero timeout. No scheduled wake source.
- Vercel project: `prj_XAoQDPZRzjK0NrqJQtwNRFOlybMN` under `nathanpannells-projects`. Production application secrets configured. Git auto-deploy disabled in `vercel.json` before integration.
- Application secrets: stored in the user's private secrets directory outside the repository; never committed.
- Verified so far: migrations applied to Neon, development seed, recommendation/import/auth tests, CSV import integration against development branch, local authenticated contact/suggestion routes, interaction create/history/delete.
