# Kinship UI refresh

- User brief: dark graphite and orange visual system, more intentional CRM layout, accessibility and color fixes, sign out, public API explanation, three-step first-login guide, import entry in Settings and empty state, public README.
- Primary reference: Attio's sidebar, dense view controls, and record context; folk's relationship follow-up model. Preserve Kinship's own content and palette.
- Branch: `feat/kinship-ui-refresh`.
- Files expected to change: `src/app/globals.css`, `src/components/app-shell.tsx`, `src/components/today-screen.tsx`, `src/components/people-screen.tsx`, `src/app/settings/page.tsx`, `src/app/settings/delete-account.tsx`, `src/app/api/auth/logout/route.ts`, public shell/home styles, new public developers page, welcome flow and migration, `README.md`. No production route deletion planned.
- Current audit: default palette is light; dark palette exists only under OS preference. Settings uses fixed white background and dark text. Public promise card also fixes a white background. Shell has no sign-out. Empty home redirects straight to import.
- Delivery gates: build/test/lint, isolated DB migration, before/after screenshots, browser checks for first-run and populated accounts on preview or stable environment, production deploy, browser smoke on deployed URL, PR evidence.
- Implementation: dark graphite/copper app shell, public home and API guide, Settings import and sign-out, empty-state import, first-login guide, README. Review also fixed the sign-in card, API response panel, skip link, and form error announcements.
- Local checks: 61 tests passed; TypeScript production build passed before final login/focus fixes; lint passed. A final build is in progress.
- Isolated browser QA: Neon schema-only branch `br-royal-brook-akmb5tkp`, database `kinship_ui_qa`, five fictional contacts and one empty user. Local support files are under `C:\repo\networking-crm-workspace` and excluded from Git. Baseline screenshots captured from `main` against this database; app branch screenshots captured with the same fictional data. Chrome dev-server origin blocked hydration on 127.0.0.1, so switch to a production-mode local server for interaction testing.
- Teardown owner: root. Delete the exact QA Neon branch and remove the baseline worktree and local support files after production browser verification and PR screenshot evidence.
- Final local verification: production-mode build and lint pass; 61 tests pass with 3 pre-existing database integration skips. Chrome confirmed the first-login flow, Settings import link, sign out, public API guide, desktop pages, mobile People selection, mobile Settings, no horizontal overflow, and no page errors. Cropped before/after screenshots with fictional data are in `docs/qa/ui-refresh`.
